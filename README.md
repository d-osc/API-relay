# API Relay

API Relay เป็นโปรแกรมที่ออกแบบมาเพื่อเชื่อมต่อและถ่ายทอดข้อมูลระหว่างผู้ให้บริการ AI ต่างๆ (เช่น ChatGPT, Perplexity) ผ่านส่วนขยาย (extension) ของเบราว์เซอร์ โปรแกรมนี้ทำงานในลักษณะของสะพานเชื่อม (bridge) ที่ช่วยให้คุณสามารถเข้าถึงความสามารถของ AI หลายๆ ตัวผ่านอินเทอร์เฟซเดียวกันได้

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Node.js Version](https://img.shields.io/badge/node-%3E%3D16.0.0-brightgreen)](https://nodejs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-4.9+-blue)](https://www.typescriptlang.org/)

## คุณสมบัติหลัก

- 🌐 **รองรับผู้ให้บริการ AI หลายราย**: ChatGPT, Perplexity และสามารถเพิ่มผู้ให้บริการอื่นๆ ได้
- 🔄 **รองรับทั้งแบบสตรีมและแบบข้อมูลเต็ม**: รับข้อมูลตอบกลับจาก AI แบบเรียลไทม์หรือแบบรอคำตอบเต็ม
- 🎯 **เข้ากันได้กับ OpenAI API**: ใช้รูปแบบที่เข้ากันได้กับ OpenAI API และ Anthropic API
- 🚀 **ง่ายต่อการติดตั้งและใช้งาน**: ติดตั้งง่ายด้วย npm และส่วนขยายเบราว์เซอร์
- 📊 **รองรับการจัดการคิว**: จัดการคำขอหลายรายการพร้อมกันอย่างเป็นระบบ
- 🛡️ **ปลอดภัย**: รองรับการจัดการความปลอดภัยและการจำกัดสิทธิ์การเข้าถึง

## โครงสร้างของโปรเจกต์

```
API-relay/
├── src/                    # Server-side code
│   ├── server.ts           # Main HTTP server
│   ├── ws.ts               # WebSocket server
│   ├── queue.ts            # Request queue management
│   ├── settings.ts         # Configuration settings
│   └── types.d.ts          # TypeScript type definitions
├── extension/              # Browser extension
│   ├── manifest.json       # Extension manifest
│   ├── background.js       # Background script
│   ├── content.js          # Content script
│   ├── provider-utils.js   # Utility functions
│   └── providers/          # Provider-specific code
│       ├── chatgpt.js      # ChatGPT provider
│       └── perplexity.js   # Perplexity provider
├── docs/                   # Documentation
│   ├── user-manual-thai.md # User manual (Thai)
│   ├── api-reference-thai.md # API documentation (Thai)
│   ├── quick-start-thai.md # Quick start guide (Thai)
│   ├── architecture-explanation-thai.md # Architecture explanation (Thai)
│   └── flow-diagrams.md    # System flow diagrams (Thai)
├── scripts/                # Utility scripts
├── memory-bank/            # Development notes
├── package.json            # Node.js dependencies
├── tsconfig.json           # TypeScript configuration
└── README.md               # This file
```

## การติดตั้งและเริ่มต้น

### ข้อกำหนดเบื้องต้น

- Node.js (รุ่น 16 ขึ้นไป)
- เบราว์เซอร์ที่รองรับส่วนขยาย (Chrome, Firefox, Edge)
- บัญชีผู้ใช้กับผู้ให้บริการ AI (เช่น ChatGPT, Perplexity)

### 1. การติดตั้งโปรเจกต์

```bash
# โคลนโปรเจกต์
git clone https://github.com/d-osc/API-relay.git

# เข้าสู่โฟลเดอร์โปรเจกต์
cd API-relay

# ติดตั้ง dependencies
npm install
```

### 2. การติดตั้งส่วนขยายเบราว์เซอร์

สำหรับ **Chrome** หรือ **Edge**:

1. เปิดเบราว์เซอร์และไปที่ `chrome://extensions/` (หรือ `edge://extensions/`)
2. เปิดโหมด "นักพัฒนาซอฟต์แวร์" (Developer mode)
3. คลิก "โหลดส่วนขยายที่ไม่ได้บรรจุ" (Load unpacked)
4. เลือกโฟลเดอร์ `extension` ในโปรเจกต์ของคุณ

### 3. การเริ่มต้นเซิร์ฟเวอร์

```bash
# เริ่มต้นเซิร์ฟเวอร์ในโหมดพัฒนา
npm run dev

# หรือสร้างไฟล์รันไทม์และเริ่มต้นเซิร์ฟเวอร์
npm run build
npm start
```

### 4. การเชื่อมต่อกับผู้ให้บริการ AI

1. เปิดหน้าเว็บของผู้ให้บริการ AI (เช่น https://chat.openai.com หรือ https://www.perplexity.ai)
2. ล็อกอินเข้าสู่บัญชีของคุณ
3. ส่วนขยายจะทำงานอัตโนมัติเมื่อตรวจพบหน้าเว็บที่รองรับ

## การใช้งาน

### การใช้งานกับ cURL

```bash
# ส่งข้อความไปยัง ChatGPT
curl -X POST http://localhost:3000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-3.5-turbo",
    "messages": [
      {"role": "user", "content": "Hello, world!"}
    ]
  }'
```

### การใช้งานกับ JavaScript

```javascript
// ส่งข้อความไปยัง AI
fetch('http://localhost:3000/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'gpt-3.5-turbo',
    messages: [
      { role: 'user', content: 'Hello, world!' }
    ]
  })
})
.then(response => response.json())
.then(data => {
  console.log(data.choices[0].message.content);
});
```

### การใช้งานแบบสตรีม (Streaming)

```javascript
// ส่งข้อความแบบสตรีม
fetch('http://localhost:3000/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'gpt-3.5-turbo',
    messages: [
      { role: 'user', content: 'Write a story about AI' }
    ],
    stream: true
  })
})
.then(response => {
  const reader = response.body.getReader();
  const decoder = new TextDecoder();

  function readStream() {
    reader.read().then(({ done, value }) => {
      if (done) return;
      
      const chunk = decoder.decode(value);
      console.log(chunk);
      
      readStream();
    });
  }
  
  readStream();
});
```

## API Documentation

สำหรับข้อมูลเพิ่มเติมเกี่ยวกับ API โปรดดูที่:
- [API Reference (Thai)](./docs/api-reference-thai.md)
- [Quick Start Guide (Thai)](./docs/quick-start-thai.md)
- [User Manual (Thai)](./docs/user-manual-thai.md)

## การตั้งค่า

คุณสามารถปรับแต่งการทำงานของโปรแกรมผ่านไฟล์ `src/settings.ts`:

```typescript
export const settings = {
  // เปลี่ยนพอร์ตเซิร์ฟเวอร์
  port: process.env.PORT || 3000,
  
  // การตั้งค่าอื่นๆ
  timeout: 180000, // 3 นาที
  maxQueueSize: 10,
  concurrentLimit: 2
};
```

## ผู้ให้บริการที่รองรับ

### ChatGPT (OpenAI)
- รองรับ GPT-3.5 Turbo, GPT-4, GPT-4 Turbo
- รองรับทั้งแบบข้อความและสตรีม
- รองรับฟังก์ชันการทำงานต่างๆ ของ ChatGPT

### Perplexity
- รองรับโมเดลต่างๆ ของ Perplexity (Turbo, Experimental, Claude 2, ฯลฯ)
- รองรับทั้งแบบข้อความและสตรีม
- รองรับฟังก์ชันการค้นหาและตอบคำถาม

## การพัฒนาและการทดสอบ

### การทดสอบด้วย npm scripts

```bash
# รันโปรแกรมในโหมดพัฒนา
npm run dev

# สร้างไฟล์รันไทม์
npm run build

# รันโปรแกรมในโหมดการผลิต
npm start

# ทดสอบ integration
npm run test:integration
```

### การทดสอบด้วย Mock Client

```bash
# ทดสอบด้วย Mock Extension Client
node scripts/mock_extension_client.js
```

## สถาปัตยกรรมของระบบ

สำหรับข้อมูลเพิ่มเติมเกี่ยวกับสถาปัตยกรรมของระบบ โปรดดูที่:
- [Architecture Explanation (Thai)](./docs/architecture-explanation-thai.md)
- [System Flow Diagrams (Thai)](./docs/flow-diagrams.md)

## การมีส่วนร่วม (Contributing)

เรายินดีต้อนรับการมีส่วนร่วมจากชุมชน! หากคุณต้องการช่วยพัฒนาโปรเจกต์นี้:

1. Fork โปรเจกต์นี้
2. สร้าง branch สำหรับฟีเจอร์ของคุณ (`git checkout -b feature/amazing-feature`)
3. Commit การเปลี่ยนแปลงของคุณ (`git commit -m 'Add some amazing feature'`)
4. Push ไปยัง branch ของคุณ (`git push origin feature/amazing-feature`)
5. เปิด Pull Request

### แนวทางการมีส่วนร่วม

- ทำตามรูปแบบโค้ดที่มีอยู่
- เพิ่มการทดสอบสำหรับฟีเจอร์ใหม่
- อัปเดตเอกสารประกอบหากจำเป็น
- ตรวจสอบให้แน่ใจว่าการทดสอบผ่านทั้งหมด

## ปัญหาที่พบบ่อยและวิธีแก้ไข

### ส่วนขยายไม่ทำงาน
- ตรวจสอบว่าได้ติดตั้งส่วนขยายในโหมดนักพัฒนาซอฟต์แวร์แล้ว
- รีเฟรชหน้าเว็บที่ต้องการใช้งาน
- ตรวจสอบในคอนโซลของเบราว์เซอร์ว่ามีข้อความแสดงความผิดพลาดหรือไม่

### เชื่อมต่อไม่ได้
- ตรวจสอบว่าเซิร์ฟเวอร์ทำงานอยู่
- ยืนยันว่าใช้พอร์ตที่ถูกต้อง
- ตรวจสอบไฟร์วอลล์และการตั้งค่าความปลอดภัย

### ข้อมูลตอบกลับไม่ครบถ้วน
- เพิ่มค่า timeout ในการตั้งค่า
- ตรวจสอบความเร็วในการเชื่อมต่ออินเทอร์เน็ต
- ยืนยันว่าใช้โมเดลที่รองรับฟังก์ชันที่ต้องการ

สำหรับข้อมูลเพิ่มเติม โปรดดูที่ [คู่มือการใช้งาน (ภาษาไทย)](./docs/user-manual-thai.md)

## ใบอนุญาต

โปรเจกต์นี้ใช้ใบอนุญาต MIT ดูข้อมูลเพิ่มเติมที่ [LICENSE](LICENSE) ไฟล์

## การติดต่อและการสนับสนุน

หากคุณมีคำถามหรือต้องการการสนับสนุน:
- รายงานปัญหา: [GitHub Issues](https://github.com/d-osc/API-relay/issues)
- คำถาม: [GitHub Discussions](https://github.com/d-osc/API-relay/discussions)
- อีเมล: d-osc@github.com

## ขอบคุณ

- ทีมงาน OpenAI สำหรับ ChatGPT API
- ทีมงาน Perplexity สำหรับบริการ AI ที่ยอดเยี่ยม
- ชุมชน open-source ที่ให้การสนับสนุนและแรงบันดาลใจ

---

⭐ หากโปรเจกต์นี้มีประโยชน์กับคุณ โปรดให้ดาว repo นี้!